name: transcribe
channels:
  - conda-forge
dependencies:
  - python=3.12           # Safe for WhisperX
  - ffmpeg                # Audio tools
  - psycopg2              # Database
  - pandas
  - jupyterlab
  - ipywidgets
  - jupyterlab_widgets
  - pip                   # We need pip for Step 2 not on conda